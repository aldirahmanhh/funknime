# Copy this file to .env.local for local dev (do NOT commit .env.local)
# Vercel does not auto-import .env from the UI; you can copy/paste each line into
# Project Settings -> Environment Variables, or use Vercel CLI.

# --- Upstream base URLs ---
SANKA_ANIME_BASE_URL=https://www.sankavollerei.com/anime
SANKA_COMIC_BASE_URL=https://www.sankavollerei.com/comic

# --- NextAuth ---
# Generate once for production, keep secret.
NEXTAUTH_SECRET=REPLACE_WITH_RANDOM_32+_CHARS
# After first deploy, set this to your actual Vercel URL
NEXTAUTH_URL=https://YOUR_PROJECT_NAME.vercel.app

# --- Database (Neon Postgres) ---
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# --- GitHub OAuth (optional, enable login) ---
GITHUB_ID=
GITHUB_SECRET=
